% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sp_list.R
\name{pull_sp_list_cols}
\alias{pull_sp_list_cols}
\title{Pull a named index of list columns matching a column type}
\usage{
pull_sp_list_cols(
  sp_list_meta,
  col_type = c("required", "hidden", "indexed", "readOnly", "boolean", "calculated",
    "choice", "contentApprovalStatus", "currency", "dateTime", "geolocation",
    "hyperlinkOrPicture", "lookup", "number", "personOrGroup", "term", "text",
    "thumbnail", "all", "editable", "external"),
  names_from = c("name", "displayName"),
  call = caller_env()
)
}
\arguments{
\item{sp_list_meta}{List column metadata returned by
\code{\link[=get_sp_list_metadata]{get_sp_list_metadata()}}: either a data frame (\code{as_data_frame = TRUE}) or
a list with one element per column (\code{as_data_frame = FALSE}).}

\item{col_type}{Column type or property to match:
\itemize{
\item "hidden", "indexed", "readOnly", or "required" match columns where the
logical property of the same name is \code{TRUE} (\code{NA} is treated as
\code{FALSE}).
\item A column type from \code{sp_list_col_types}, e.g. "text", "number", or
"lookup", matches columns with the column type property (facet) of the
same name. For data frame input, a column matches if the
nested facet data frame has any non-missing value for that column.
"boolean", "contentApprovalStatus", "geolocation", and "thumbnail"
columns can only be identified from list input because these facets are
empty objects (\code{{}}) that are dropped when the API response is
simplified to a data frame.
\item "all" matches every column.
\item "editable" matches columns where \code{readOnly} is not \code{TRUE}.
\item "external" matches columns with a name not in
\code{sp_list_internal_colnames}.
}}

\item{names_from}{Property to use for names of the returned vector. One of
"name" (default) or "displayName".}
}
\value{
A named integer vector of positions in \code{sp_list_meta} for the
matching columns, named with the corresponding values of \code{names_from}.
Returns a zero-length named integer vector if no columns match or if
\code{sp_list_meta} lacks the metadata needed to identify \code{col_type}.
}
\description{
Find the columns described by list metadata from \code{\link[=get_sp_list_metadata]{get_sp_list_metadata()}}
that match a single type or property, e.g. all lookup columns or all
required columns. Pass the result to \code{\link[vctrs:vec_slice]{vctrs::vec_slice()}} to subset
\code{sp_list_meta}.
}
\keyword{internal}
