# Format a data frame with specified person columns to create or update person/group columns in a SharePoint list

Format a data frame with specified person columns to create or update
person/group columns in a SharePoint list

## Usage

``` r
fmt_person_lookup_id_values(
  .data,
  cols,
  users_id = "EMail",
  site_users = NULL,
  sp_site = NULL
)
```
